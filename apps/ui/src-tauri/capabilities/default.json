{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only the local Nemo window may call the desktop connection command.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
